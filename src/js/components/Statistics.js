export default class Statistics {
    constructor(localStorageDate, graphDate, request) {
        this.localStorageDate = localStorageDate;
        this.graphDate = graphDate;
        this.request = request;
    }

}